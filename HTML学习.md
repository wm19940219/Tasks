关于HTML！
哇哇哇！这个可以创建网页，我已经完成了第一个网页的创建！

## 框架

我发现 **HTML** 写起来其实是有模板的  
标题（**Heading**）是通过`<h1> - <h6>` 标签来定义的。  
段落是通过标签 `<p>` 来定义的。  
链接是通过标签 `<a>` 来定义的。（提示:在 **href** 属性中指定链接的地址。）  
图像是通过标签 `<img>` 来定义的.（注意： 图像的名称和尺寸是以属性的形式提供的。）  
即房子已经全部弄好了，装修由你去完成，你可以在这里进行创作！
